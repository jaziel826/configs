
########################################################################################
AUTOGENERATED HYPR CONFIG.
PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

#autogenerated = 1 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=eDP-1,1920x1080@60,0x0,1


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec = pkill waybar && waybar -c $HOME/.config/waybar/config-hypr -s $HOME/.config/waybar/hypr.css
#exec = rm -f $WOBSOCK && mkfifo $WOBSOCK && tail -f $WOBSOCK | wob
$wall = /home/jaziel/.config/hypr/scripts/swww_randomize.sh /home/jaziel/.config/wall/  
exec-once = swww init
exec-once=zellij attach -c Main
#/home/jaziel/.config/hypr/scripts/randomwall.sh 
exec-once = $wall
exec-once = dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY
exec = $HOME/.config/hypr/scripts/idle.sh
$lock=~/.config/hypr/scripts/lock.sh
# Sound 
bind=,XF86AudioRaiseVolume, exec, $HOME/.config/hypr/scripts/volume.sh --inc
#pactl set-sink-volume @DEFAULT_SINK@ +5% && pactl get-sink-volume @DEFAULT_SINK@ | head -n 1| awk '{print substr($5, 1, length($5)-1)}' > $WOBSOCK
bind=,XF86AudioLowerVolume, exec, $HOME/.config/hypr/scripts/volume.sh --dec
#pactl set-sink-volume @DEFAULT_SINK@ -5% && pactl get-sink-volume @DEFAULT_SINK@ | head -n 1 | awk '{print substr($5, 1, length($5)-1)}' > $WOBSOCK
bind=,XF86AudioMute, exec, $HOME/.config/hypr/scripts/volume.sh --toggle
#pactl set-sink-mute @DEFAULT_SINK@ toggle
bind=,XF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle

# LIGHT
bind=,XF86MonBrightnessUp, exec, $HOME/.config/hypr/scripts/brightness.sh --inc
#light -A 5 && light -G | cut -d'.' -f1 > $WOBSOCK
bind=,XF86MonBrightnessDown, exec, $HOME/.config/hypr/scripts/brightness.sh --dec
#light -U 5 && light -G | cut -d'.' -f1 > $WOBSOCK

# clipboard 
exec-once = wl-paste -t text --watch clipman store --no-persist
exec-once = wl-paste -p -t text --watch clipman store -P --histpath="~/.local/share/clipman-primary.json"
# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 10
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 5
    blur = yes
    blur_size = 3
    blur_passes = 1
    blur_new_optimizations = on

    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = false
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = on
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:epic mouse V1 {
    sensitivity = -0.5
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, T, exec, foot
bind = $mainMod, Q, killactive, 
bind = $mainMod, M, exit, 
bind = $mainMod, E, exec, dolphin
bind = $mainMod, V, togglefloating, 
bind = $mainMod, Space, exec, fuzzel
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, B, exec, firefox
bind = $mainMod SHIFT, DELETE, exec, wlogout -p layer-shell
bind = $mainMod, RETURN, exec, foot -e zellij attach Main 
#tmux new-session -A -s main
bind = ALT, C, exec, clipman pick -t CUSTOM -T "fuzzel -d -f JetBrains Mono:9"
# bind = $mainMod, N, exec, swaync-client -t
bind=,switch:on:Lid Switch, exec, $lock 
bind=$mainMod, TAB, workspace, e+1
bind=$mainMod SHIFT, TAB, workspace, e-1
bind=$mainMod, D, exec, swaync-client --hide-latest
bind=$mainMod, N, exec, swaync-client -t

#WOBSOCK=$XDG_RUNTIME_DIR/wob.sock
#XDG_CURRENT_DESKTOP=Hyprland
#XDG_SESSION_TYPE=wayland
#XDG_SESSION_DESKTOP=Hyprland
#QT_QPA_PLATFORM="wayland;xcb"

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d
bind = $mainMod Shift, >, cyclenext

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# will switch to a submap called resize
bind=ALT,R,submap,resize

# will start a submap called "resize"
submap=resize

# sets repeatable binds for resizing the active window
binde=,right,resizeactive,10 0
binde=,left,resizeactive,-10 0
binde=,up,resizeactive,0 -10
binde=,down,resizeactive,0 10

# use reset to go back to the global submap
bind=,escape,submap,reset 

# will reset the submap, meaning end the current one and return to the global one
submap=reset

# keybinds further down will be global again...

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1
windowrule = float, file_progress
windowrule = float, confirm
windowrule = float, dialog
windowrule = float, download
windowrule = float, notification
windowrule = float, error
windowrule = float, splash
windowrule = float, confirmreset
#windowrule = fullscreen, wleave
#windowrule = float, title:wleave
#windowrule = fullscreen, title:wleave
windowrule = animation none, title:wlogout
windowrule = fullscreen, wlogout
windowrule = float, title:wlogout
windowrule = float, title:protonvpn
windowrule = fullscreen, title:wlogout
windowrule = idleinhibit focus, mpv
windowrule = idleinhibit fullscreen, firefox
windowrule = float, title:^(Picture-in-Picture|Picture in picture)$
windowrule = pin, title:^(Picture-in-Picture|Picture in picture)$
windowrule = move 1400 50%, title:^(Picture-in-Picture|Picture in picture)$
windowrule = size 500 280, title:^(Picture-in-Picture|Picture in picture)$
windowrule = float, title:Bluetooth Devices
windowrule = size 550 480, title:Bluetooth Devices
windowrule = pin, title:Bluetooth Devices
windowrule = float, title:Authentication Required — PolicyKit1 KDE Agent
windowrule = pin, title:^(Albert)$
windowrule = noborder, title:^(Albert)$
windowrule = float, title:^(Albert)$
windowrule = fullscreen, title:^(Albert)$
windowrule = dimaround, title:^(Albert)$
windowrule = pin, title:^(waybar)$
windowrule = maximize, ^(mpv)$
windowrule = dimaround,^(rofi)$

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
exec-once=xprop -root -f _XWAYLAND_GLOBAL_OUTPUT_SCALE 32c -set _XWAYLAND_GLOBAL_OUTPUT_SCALE 2
#exec-once = swaync
exec-once = swaync
exec-once=/usr/lib/polkit-kde-authentication-agent-1
exec-once = sway-autostart
exec-once = blueman-applet
exec-once = kdeconnect-app
exec-once = nm-applet 
exec-once = protonmail-bridge
exec-once = thunderbird
include ~/.config/sway/.generated_settings/*.conf
